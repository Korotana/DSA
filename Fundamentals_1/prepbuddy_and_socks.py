# https://mycode.prepbytes.com/problems/fundamentals/MAXDRAWS

testcases = [input() for i in range(int(input()))]

def check_socks_draws(pairs):
    return pairs+1

for pairs in testcases:
    print(check_socks_draws(int(pairs)))